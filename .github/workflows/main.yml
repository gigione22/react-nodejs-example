name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello from CI"
  script:
    - java -version
    - mvn test
    
